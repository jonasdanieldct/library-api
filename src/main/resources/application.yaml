spring:
  datasource:
    url: jdbc:mysql://localhost:3320/global_db?useSSL=false&serverTimezone=UTC
    username: jddc
    password: jddc

  jpa:
    hibernate:
      ddl-auto: none
#     tells jpa to map db objects based on what's written in entity class
#      naming:
#        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: false
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
